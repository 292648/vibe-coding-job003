# 變更紀錄

## [0.1.0] - Step 1: 基本的新增、完成、刪除、未完成計數、localStorage 保存

### 新增
- 新增待辦事項輸入欄位與新增按鈕。
- 新增待辦項目列表，支援顯示待辦內容。
- 新增完成狀態勾選功能，可將任務標記為已完成或未完成。
- 新增刪除功能，可移除不需要的待辦項目。
- 新增未完成項目計數，顯示目前還有多少項目未完成。
- 新增 localStorage 持久化，讓待辦資料在重新整理後仍然保留。

### 變更
- 調整待辦項目的渲染邏輯，讓新增、完成、刪除後能即時更新畫面。
- 優化未完成數量計算邏輯，確保每次更新都依照實際狀態計算。
- 強化資料載入與儲存流程，避免因資料格式異常造成錯誤。

## [0.2.0] - Step 2: 深色模式切換(含記住偏好與跟隨系統設定)、全部/未完成/已完成篩選

### 新增
- 新增深色模式切換按鈕，放置於標題列右側。
- 新增深淺色主題切換功能，按鈕上顯示圖示與文字。
- 新增主題偏好儲存功能，使用者選擇的模式會寫入 localStorage。
- 新增系統主題跟隨邏輯，若使用者未手動設定則依照作業系統的 prefers-color-scheme 判斷。
- 新增全部、未完成、已完成三個篩選按鈕。
- 新增篩選後的空狀態提示，當對應類別沒有項目時顯示說明文字。

### 變更
- 調整整體 UI 配色，改為以 CSS 變數管理淺色與深色主題，方便維護與切換。
- 更新待辦列表的渲染流程，讓篩選行為不影響底部未完成總數。
- 讓篩選按鈕具備明確的選取狀態，提升使用者辨識度。
- 調整未完成計數邏輯，保證其永遠顯示整體未完成項目數量，而非篩選後數量。
